

{% set serviceName = 'Apply for Pension Credit' %}
{% extends "/layouts/main.html" %}

{% set pageName = "Backdate your application" %}

{% block beforeContent %}
{{ govukPhaseBanner({
  tag: {
    text: "Beta"
  },
  html: 'This is a new service – your <a class="govuk-link" href="#">feedback</a> will help us to improve it.'
}) }}
{% endblock %}

{% block content %}

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    <p><a href="javascript:history.back()" class="govuk-back-link govuk-!-static-margin-0">Back</a></p>

    <form method="post" >
 <div class="govuk-form-group">
        <fieldset class="govuk-fieldset" aria-describedby="changedName-hint">
      <legend class="govuk-fieldset__legend govuk-fieldset__legend--l">
          <h1 class="govuk-fieldset__heading">
          Backdate your application
      </h1>
        </legend>


<p class="govuk-body">
                    We may be able to backdate your application to <span class="no-wrap">{{data.backdateDateString | govukDate}}</span>.</p>
            <p class="govuk-body">We can assess your application from a later date if your circumstances have changed more recently.

          </p>

          <div class="govuk-form-group">
                  <fieldset class="govuk-fieldset">
                    <legend class="govuk-fieldset__legend govuk-fieldset__legend--m">
  What date do you want us to assess your application from?                    </legend>



          <div class="govuk-radios govuk-radios" data-module="govuk-radios">
            <div class="govuk-radios__item">
                <input class="govuk-radios__input" id="standardBackdating" name="standardBackdating" type="radio" value="yes">
                <label class="govuk-label govuk-radios__label" for="standardBackdating">
                <span class="no-wrap"><span class="no-wrap">{{data.backdateDateString | govukDate}}</span>
                </label>
                <div id="signIn-item-hint" class="govuk-hint govuk-radios__hint">
          We'll backdate your application to this date or the earliest possible date after.
            </div>
            </div>
            <div class="govuk-radios__item">
                <input class="govuk-radios__input" id="standardBackdating-2" name="standardBackdating" type="radio" value="no">
                <label class="govuk-label govuk-radios__label" for="standardBackdating-2">
              A later date
                </label>
                <div id="signIn-item-hint" class="govuk-hint govuk-radios__hint">
         You can choose a date after <span class="no-wrap">{{data.backdateDateString | govukDate}}</span>.
       </div>
            </div>
          </div>
        </fieldset>
      </div>

      <button type="submit" class="govuk-button" data-module="govuk-button">
          Continue
      </button>

    </form>
    {% include '../partials/gethelp.html' %}
  </div>
</div>

{% endblock %}
